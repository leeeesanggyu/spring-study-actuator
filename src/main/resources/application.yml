management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
#      show-components: always

  endpoints:
    web:
      exposure:
        include: "*"

info:
  test:
    code: 200
    message: hello, world!

# Can be changed by API request during Runtime
logging:
  level:
    hello.controller: debug